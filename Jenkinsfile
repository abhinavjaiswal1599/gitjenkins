pipeline {
    agent any

    stages {
        stage('Compile') {
            steps {
                echo 'no need to build python code'
            }
        }
      stage('first'){
        steps{
          sh "python3 first.py"
       }
     }
   }
}
